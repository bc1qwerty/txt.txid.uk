{
  "name": "txt.txid.uk",
  "version": "0.1.0",
  "description": "Text-only mirror of txid.uk family sites. Brutalist, HTML-first, zero-framework.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "clean": "rm -rf dist",
    "dev": "node src/build.mjs && echo 'Serve dist/ with any static server'",
    "health": "node src/health-check.mjs",
    "deploy": "npm run build && npm run build:gemini && npx wrangler pages deploy dist --project-name=txt-txid-uk --branch=main && npm run deploy:gemini-sync && npm run deploy:ipfs-sync && npm run health && notify-deploy.sh txt.txid.uk",
    "build:gemini": "node src/build-gemini.mjs",
    "deploy:gemini-sync": "bash scripts/sync-gemini.sh",
    "deploy:ipfs-sync": "bash scripts/sync-ipfs.sh"
  },
  "dependencies": {}
}
